---
tags: [Week1, 수정판, Claude Code, iOS, Android, 동시개발]
created: 2026-06-05
week: 1
status: "Apple Developer 결제 제외 + iOS·Android 동시 + Claude Code 기반"
관련: "[[아이디어/Memsum/CLAUDE|CLAUDE.md]] · [[아이디어/Memsum/디자인시스템|디자인시스템]]"
---

# Memsum Week 1 가이드 (수정판)

> 이전 W1 가이드와 차이: Apple Developer Program 결제 제외, iOS·Android 동시 개발, Claude Code 자동 진행(사용자는 명령어와 GUI 클릭만), Swift/Kotlin 학습 시간 없음.

---

## 1. Week 1 목표

이번 주가 끝났을 때 다음 상태가 되어야 한다.

- iOS 시뮬레이터·Android 에뮬레이터 **양쪽**에서 Memsum 앱이 실행된다
- iOS PhotoLibraryWatcher(Swift) + Android ContentObserver(Kotlin) 양쪽에서 스크린샷 이벤트가 RN 콘솔에 로그로 찍힌다
- Supabase 초기 스키마(user_profiles, captures + RLS)가 적용되어 있고 더미 INSERT가 성공한다
- Sentry / PostHog 첫 이벤트가 대시보드에 도착한다
- Claude Code 한 줄 지시 → 결과 확인 → 다음 지시 흐름이 몸에 익는다

**이번 주 하지 않는 것 (다음 단계로 미룸)**
- Apple Developer Program 결제 ($99) — 실기기·TestFlight 필요할 때 결제
- App Store Connect 셋업
- EAS Submit (스토어 업로드). preview 빌드까지만.
- 실기기 빌드 — 시뮬레이터·에뮬레이터에서 끝낸다

---

## 2. Day 1~7 일일 액션

각 날짜는 (1) Claude Code 한 줄 지시 → (2) 결과 확인 포인트 → (3) 사용자 GUI 작업 → (4) 산출물 순서다.

### Day 1 — 프로젝트 셋업

**Claude Code 지시**
```
"Memsum Day 1 시작해줘. CLAUDE.md 따라 Expo 프로젝트 만들고 prebuild까지."
```

**Claude Code가 실행하는 것**
```bash
npx create-expo-app@latest memsum --template blank-typescript
cd memsum
npx expo install expo-dev-client
npx expo prebuild --platform all
npm i @sentry/react-native posthog-react-native @supabase/supabase-js
```

**사용자 확인**
- `memsum/ios/` 와 `memsum/android/` 폴더가 모두 생성됐는지
- `package.json`에 expo SDK 51+ 표시

**사용자 GUI 작업**
- GitHub에서 Private repo `memsum` 생성 → Claude Code가 `git remote add origin ... && git push`
- Sentry: sentry.io 가입 → 프로젝트 생성 → DSN 복사 → `.env`
- PostHog: 가입 → API key 복사 → `.env`

**산출물**
- `memsum/` Expo 프로젝트, ios·android 네이티브 폴더, GitHub Private repo, `.env`

---

### Day 2 — Supabase + 외부 계정

**Claude Code 지시**
```
"Day 2 가자. Supabase 클라이언트 코드 짜고 스키마 SQL 준비해줘."
```

**사용자 GUI 작업 (직접)**
1. supabase.com → New Project → 이름 `memsum`, region `ap-northeast-2`
2. 프로젝트 생성 후 `Settings → API`에서 `URL`, `anon key`, `service_role` 복사 → `.env`
3. platform.openai.com → API Keys → 새 키 발급 (카드 등록) → `.env`에 `OPENAI_API_KEY`

**Claude Code가 실행**
- `lib/supabase.ts` 클라이언트 작성
- `supabase/migrations/0001_init.sql` 작성 (5번 항목 스키마)
- Supabase Dashboard `SQL Editor`에 붙여넣을 SQL 출력

**사용자 확인**
- Supabase Table Editor에 `user_profiles`, `captures` 테이블
- `INSERT INTO captures (...)` 더미 한 줄 성공

---

### Day 3 — iOS·Android 동시 첫 빌드

**Claude Code 지시**
```
"Day 3. iOS 시뮬레이터랑 Android 에뮬레이터 둘 다 띄워줘."
```

**사용자 GUI 작업 (선행)**
- Xcode 설치 (App Store) → Settings → Platforms → iOS Simulator 다운로드
- Android Studio 설치 → Device Manager → Create Device → **Pixel 6 / API 34** AVD 생성
- `~/.zshrc`에 `ANDROID_HOME` 추가는 Claude Code가 안내

**Claude Code가 실행**
```bash
# 터미널 1
npx expo run:ios --device "iPhone 15"
# 터미널 2 (에뮬레이터 먼저 부팅)
npx expo run:android
```

**사용자 확인**
- 시뮬레이터 + 에뮬레이터 두 창에 동일한 Hello Memsum 화면
- 권한 다이얼로그 뜨면 **[허용]** 탭

---

### Day 4 — 디자인 시스템 도입

**Claude Code 지시**
```
"Day 4. 디자인시스템.md 참고해서 디자인 토큰 코드 짜고 gluestack-ui 셋업해줘.
Button이랑 Card 컴포넌트 양쪽 OS에서 동작 확인까지."
```

**Claude Code가 실행**
- `src/design/tokens/` — 라벤더 보라 #7C6FE8 + 코랄 액센트, spacing 4/8/12/16/24, radius
- `src/design/theme/gluestack.config.ts` 셋업
- `src/design/components/Button/Button.tsx`, `Card/Card.tsx`
- `app/index.tsx`에 두 컴포넌트 렌더링

**사용자 확인**
- iOS·Android 양쪽에서 보라색 Button, Card가 동일 톤 렌더링
- 다크모드 토글 시 색 자동 전환

---

### Day 5 — PhotoLibraryWatcher (iOS Swift)

**Claude Code 지시**
```
"Day 5. iOS PhotoLibraryWatcher Expo Module 만들어줘.
스크린샷 감지되면 RN에 이벤트 보내는 것까지."
```

**Claude Code가 실행**
```bash
npx create-expo-module@latest --local modules/photo-library-watcher
```
- `modules/photo-library-watcher/ios/PhotoLibraryWatcherModule.swift` (4번 참조)
- `Info.plist`에 `NSPhotoLibraryUsageDescription` 추가
- TypeScript wrapper `index.ts`에서 `addListener('onScreenshot', ...)` export

**사용자 확인**
- 시뮬레이터에서 **Cmd + S** (시뮬레이터 메뉴 `File → Save Screen`)
- Metro 콘솔에 `[Screenshot] assetId=... createdAt=...` 출력

---

### Day 6 — Android ContentObserver (Kotlin)

**Claude Code 지시**
```
"Day 6. Day 5에서 만든 모듈에 Android 측 Kotlin 코드 추가해줘.
ContentObserver로 스크린샷만 필터링."
```

**Claude Code가 실행**
- `modules/photo-library-watcher/android/.../PhotoLibraryWatcherModule.kt`
- `AndroidManifest.xml`에 `READ_MEDIA_IMAGES` 권한

**사용자 확인**
- Android 에뮬레이터에서 **Ctrl + S** 또는 사이드바 카메라 아이콘
- 또는 `adb shell screencap /sdcard/Pictures/Screenshots/test.png`
- Metro 콘솔에 `[Screenshot] uri=content://... displayName=Screenshot_...` 출력

---

### Day 7 — 회고 + W2 준비

**Claude Code 지시**
```
"Day 7. Week 1 검증 체크리스트 돌려주고, 빠진 항목 정리해줘.
Week 2 OCR 작업 위해 필요한 사전 작업도 알려줘."
```

**산출물**
- 회고 노트 (Obsidian): 잘 된 것 / 막힌 것 / Week 2 사전 준비
- main 브랜치에 Week 1 결과물 푸시 + `v0.1.0-w1` 태그

---

## 3. iOS·Android 동시 빌드하는 법

**핵심**: Metro 번들러는 하나만 띄우고, 시뮬레이터/에뮬레이터 두 개에 동시 attach.

```bash
# 사전: Android 에뮬레이터 먼저 부팅 (Android Studio Device Manager)

# 첫 빌드만 native 빌드 (둘 다 한 번씩)
npx expo run:ios       # 터미널 1
npx expo run:android   # 터미널 2

# 이후 코드 수정 시: Metro만 띄우면 양쪽 동시 hot reload
npx expo start --dev-client
# Metro 콘솔에서 i → iOS, a → Android
```

**디버깅 단축키**
- iOS 시뮬레이터: `Cmd + D` → Dev Menu
- Android 에뮬레이터: `Cmd + M` → Dev Menu
- React DevTools: `npx react-devtools`

---

## 4. PhotoLibraryWatcher 양쪽 OS 구현

### iOS (Swift) — `PhotoLibraryWatcherModule.swift`

```swift
import ExpoModulesCore
import Photos

public class PhotoLibraryWatcherModule: Module, PHPhotoLibraryChangeObserver {
  private var lastFetchResult: PHFetchResult<PHAsset>?

  public func definition() -> ModuleDefinition {
    Name("PhotoLibraryWatcher")
    Events("onScreenshot")

    OnCreate {
      PHPhotoLibrary.requestAuthorization(for: .readWrite) { _ in
        let options = PHFetchOptions()
        options.predicate = NSPredicate(
          format: "(mediaSubtype & %d) != 0",
          PHAssetMediaSubtype.photoScreenshot.rawValue
        )
        options.sortDescriptors = [NSSortDescriptor(key: "creationDate", ascending: false)]
        self.lastFetchResult = PHAsset.fetchAssets(with: .image, options: options)
        PHPhotoLibrary.shared().register(self)
      }
    }

    OnDestroy {
      PHPhotoLibrary.shared().unregisterChangeObserver(self)
    }
  }

  public func photoLibraryDidChange(_ changeInstance: PHChange) {
    guard let last = lastFetchResult,
          let changes = changeInstance.changeDetails(for: last) else { return }
    self.lastFetchResult = changes.fetchResultAfterChanges

    for asset in changes.insertedObjects {
      guard asset.mediaSubtypes.contains(.photoScreenshot) else { continue }
      sendEvent("onScreenshot", [
        "assetId": asset.localIdentifier,
        "createdAt": asset.creationDate?.timeIntervalSince1970 ?? 0
      ])
    }
  }
}
```

### Android (Kotlin) — `PhotoLibraryWatcherModule.kt`

```kotlin
package expo.modules.photolibrarywatcher

import android.database.ContentObserver
import android.net.Uri
import android.os.Handler
import android.os.Looper
import android.provider.MediaStore
import expo.modules.kotlin.modules.Module
import expo.modules.kotlin.modules.ModuleDefinition

class PhotoLibraryWatcherModule : Module() {
  private var observer: ContentObserver? = null
  private var lastSeenId: Long = 0L

  override fun definition() = ModuleDefinition {
    Name("PhotoLibraryWatcher")
    Events("onScreenshot")

    OnCreate {
      val ctx = appContext.reactContext ?: return@OnCreate
      val resolver = ctx.contentResolver

      observer = object : ContentObserver(Handler(Looper.getMainLooper())) {
        override fun onChange(selfChange: Boolean, uri: Uri?) {
          super.onChange(selfChange, uri)
          val projection = arrayOf(
            MediaStore.Images.Media._ID,
            MediaStore.Images.Media.DISPLAY_NAME,
            MediaStore.Images.Media.RELATIVE_PATH,
            MediaStore.Images.Media.DATE_ADDED
          )
          resolver.query(
            MediaStore.Images.Media.EXTERNAL_CONTENT_URI,
            projection, null, null,
            "${MediaStore.Images.Media.DATE_ADDED} DESC LIMIT 1"
          )?.use { cursor ->
            if (cursor.moveToFirst()) {
              val id = cursor.getLong(0)
              val name = cursor.getString(1) ?: ""
              val path = cursor.getString(2) ?: ""
              if (id == lastSeenId) return@use
              val isScreenshot = path.contains("Screenshots", true) ||
                                 name.contains("Screenshot", true)
              if (!isScreenshot) return@use
              lastSeenId = id
              val contentUri = Uri.withAppendedPath(
                MediaStore.Images.Media.EXTERNAL_CONTENT_URI, id.toString()
              )
              sendEvent("onScreenshot", mapOf(
                "uri" to contentUri.toString(),
                "displayName" to name,
                "createdAt" to cursor.getLong(3)
              ))
            }
          }
        }
      }
      resolver.registerContentObserver(
        MediaStore.Images.Media.EXTERNAL_CONTENT_URI, true, observer!!
      )
    }

    OnDestroy {
      observer?.let {
        appContext.reactContext?.contentResolver?.unregisterContentObserver(it)
      }
    }
  }
}
```

### TypeScript wrapper — `modules/photo-library-watcher/index.ts`

```ts
import { EventEmitter, NativeModule, requireNativeModule } from 'expo';

type ScreenshotEvent = {
  assetId?: string;
  uri?: string;
  displayName?: string;
  createdAt: number;
};

declare class PhotoLibraryWatcherModule extends NativeModule<{
  onScreenshot: (e: ScreenshotEvent) => void;
}> {}

const Native = requireNativeModule<PhotoLibraryWatcherModule>('PhotoLibraryWatcher');

export function addScreenshotListener(cb: (e: ScreenshotEvent) => void) {
  return Native.addListener('onScreenshot', cb);
}
```

---

## 5. Supabase 초기 스키마

```sql
-- supabase/migrations/0001_init.sql
create table public.user_profiles (
  id uuid primary key references auth.users on delete cascade,
  display_name text,
  timezone text default 'Asia/Seoul',
  created_at timestamptz default now()
);

create table public.captures (
  id uuid primary key default gen_random_uuid(),
  user_id uuid not null references auth.users on delete cascade,
  image_url text not null,
  ocr_text text,
  parsed_event jsonb,
  source_platform text check (source_platform in ('ios','android')),
  status text default 'pending' check (status in ('pending','ocr_done','calendar_added','failed')),
  created_at timestamptz default now()
);

create index on public.captures (user_id, created_at desc);

alter table public.user_profiles enable row level security;
alter table public.captures enable row level security;

create policy "own profile" on public.user_profiles
  for all using (auth.uid() = id);

create policy "own captures" on public.captures
  for all using (auth.uid() = user_id);
```

---

## 6. Claude Code 작업 흐름 (한 줄 지시 패턴)

| 사용자 한 줄 | Claude Code 응답 흐름 |
|---|---|
| `"Memsum Day 1 시작해줘"` | CLAUDE.md·디자인시스템.md 읽기 → Day 1 산출물 목록 → 명령 실행 |
| `"Day 1 끝났어. Day 2 가자"` | git status 확인 → 누락 시 커밋 → Day 2 GUI 안내 → 키 받으면 코드 작성 |
| `"에러 났어: [에러 메시지]"` | 에러 원문 파싱 → 원인 3개 → 가장 가능성 높은 것부터 명령 제시 |
| `"Android 에뮬레이터에서 스크린샷 어떻게 찍어?"` | Ctrl+S / Extended Controls / adb 3가지 안내 |
| `"디자인시스템.md 보고 Button 만들어줘"` | tokens.ts 확인 → Button.tsx 생성 → 양쪽 OS 렌더링 |
| `"PhotoLibraryWatcher 안 동작해. 진단해줘"` | (1) Info.plist (2) 권한 다이얼로그 (3) Module 등록 (4) Metro 재시작 체크리스트 |

**원칙**: 사용자는 코드를 직접 안 짠다. 명령어 실행, 에러 메시지 전달, GUI 클릭만.

---

## 7. 사용자가 직접 클릭해야 할 GUI 작업

- [ ] GitHub Private repo `memsum` 생성
- [ ] Supabase 프로젝트 생성 + URL/anon/service_role 키 복사
- [ ] Supabase SQL Editor에 마이그레이션 SQL 붙여넣고 Run
- [ ] OpenAI API 키 발급 (카드 등록)
- [ ] Sentry 프로젝트 생성 + DSN 복사
- [ ] PostHog 프로젝트 생성 + API key 복사
- [ ] Xcode 설치 + iOS 시뮬레이터 다운로드
- [ ] Android Studio 설치 + AVD (Pixel 6 / API 34) 생성
- [ ] iOS 시뮬레이터 첫 실행 시 사진 권한 다이얼로그 **[허용]**
- [ ] Android 에뮬레이터 첫 실행 시 미디어 권한 다이얼로그 **[허용]**
- [ ] (이번 주 안 함) Apple Developer Program 결제 — 나중에

---

## 8. 흔한 함정 & 해결

| 함정 | 증상 | 해결 |
|---|---|---|
| prebuild 후 ios/android 커밋 안 됨 | CI/재clone 시 빌드 깨짐 | `.gitignore`에서 `ios/` `android/` 제거 후 커밋 |
| CocoaPods 캐시 꼬임 | `pod install` 무한 에러 | `cd ios && pod deintegrate && pod install --repo-update` |
| `ANDROID_HOME` 미설정 | `expo run:android` 실패 | `~/.zshrc`에 export 추가 |
| iOS 시뮬레이터 스크린샷 단축키 헷갈림 | Cmd+Shift+3 → Mac 스크린샷됨 | 시뮬레이터 `File → Save Screen` 또는 `Cmd+S` |
| Android ContentObserver 과다 트리거 | onChange 초당 수십번 | `lastSeenId` 캐시 + DISPLAY_NAME/RELATIVE_PATH 필터 |
| iOS 권한 다이얼로그 안 뜸 | requestAuthorization 호출 안 됨 | Info.plist 권한 키 확인 |
| Metro 캐시 stale | 모듈 추가했는데 인식 안 됨 | `npx expo start -c` |
| Android 13+ 미디어 권한 | READ_EXTERNAL_STORAGE 무시됨 | `READ_MEDIA_IMAGES` 사용 |

---

## 9. Week 1 종료 검증 체크리스트

- [ ] iOS 시뮬레이터에서 `expo run:ios` 빌드·실행 성공
- [ ] Android 에뮬레이터에서 `expo run:android` 빌드·실행 성공
- [ ] iOS 스크린샷(Cmd+S) → Metro 콘솔에 `[Screenshot]` 로그
- [ ] Android 스크린샷(Ctrl+S) → Metro 콘솔에 `[Screenshot]` 로그
- [ ] Supabase `user_profiles`, `captures` 테이블 생성 + RLS 활성화
- [ ] 더미 INSERT 1건 성공 (서비스 키 테스트)
- [ ] Sentry 대시보드에 첫 이벤트
- [ ] PostHog 대시보드에 첫 이벤트
- [ ] 디자인 토큰 + Button + Card 양쪽 OS 동일 렌더링
- [ ] GitHub main 브랜치에 7일치 일일 커밋
- [ ] Claude Code 한 줄 지시 → 결과 확인 루프 자연스러움
- [ ] (이번 주 미수행) Apple Developer 결제, EAS Submit, 실기기 빌드

---

## 10. 다음 주(Week 2) 예고

**Week 2 — OCR 파이프라인**
- iOS: Vision Framework로 한국어 텍스트 인식 (`VNRecognizeTextRequest`, `recognitionLanguages = ["ko-KR","en-US"]`)
- Android: ML Kit Text Recognition (Korean 모델)
- 양쪽 결과를 GPT-4o-mini로 후처리해 일정 후보(JSON)로 정규화
- 한국어 OCR 오인식 패턴 수집 + 프롬프트 튜닝
- captures 테이블 `ocr_text`, `parsed_event` 컬럼 채우기

**Week 2 시작 전 사전 준비**
- Week 1 검증 체크리스트 전부 통과
- 한국어가 포함된 테스트 스크린샷 10장 준비 (카톡 약속, 메일 일정, 웹 이벤트 페이지 등)
- OpenAI 결제 한도 확인 ($5~10 충전 권장)

---

*마스터플랜: [[아이디어/Memsum/마스터플랜|마스터플랜]] · CLAUDE.md: [[아이디어/Memsum/CLAUDE|CLAUDE.md]] · 디자인시스템: [[아이디어/Memsum/디자인시스템|디자인시스템]]*
